<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow shadow-primary">
  <div class="container-fluid text-light">
    <a class="navbar-brand" href="/">Portfolio</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link <%= currentPage === 'home' ? 'active' : '' %>" href="/">Home</a>
        <a class="nav-link <%= currentPage === 'contact' ? 'active' : '' %>" href="/contact">Contact</a>
        <% if(isAuthenticated){ %>
          <a class="nav-link <%= currentPage === 'dashboard' ? 'active' : '' %>"
            href="/atul-admin/dashboard">Dashboard</a>
          <a class="nav-link" href="/logout">Logout</a>
          <% } %>
      </div>
    </div>
  </div>
</nav>